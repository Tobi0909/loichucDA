# Anthropic API key dùng cho route /api/greeting
# Lấy tại: https://console.anthropic.com/settings/keys
# Nếu KHÔNG set biến này, web vẫn chạy bình thường và tự dùng kho lời chúc tĩnh (fail-open).
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
